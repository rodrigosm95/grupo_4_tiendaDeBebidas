<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../partial/head") %>
</head>
<body>
    <header> <%- include("../partial/header") %></header>
    <article class="sale-product">                    
        <a class="producto-destacado" href="#">
            <img src="/images/products/<%=product.imagen.url %>"  alt="<%= product.name %> ">
            <div class="descripcion-producto">
            <h4><%= product.name  %> </h4>
            <p>$<%= product.price  %> </p>
            </div>
            <form action="/products/update/<%= product.id %>">
                <button type="submit">editar producto</button>
            </form>
        </a>
    </article>

    <footer><%- include("../partial/footer") %></footer>
    
</body>
</html>